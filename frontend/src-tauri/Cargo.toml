[package]
name = "autorippr-desktop"
version = "0.1.0"
description = "Auto-Ripper desktop frontend"
authors = ["Kevin", "Copilot"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.1", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2.0.6", features = [] }

[features]
default = []
